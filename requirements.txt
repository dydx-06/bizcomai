# Core
pandas>=2.0
numpy>=1.24
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Tier 3 (optional): unset GROQ_API_KEY to disable the LLM tier entirely
groq>=0.11
python-dotenv>=1.0

# Tier 2 (recommended): without this, app/main.py falls back to HashingEmbedder,
# which cannot read real bank narration. See README "Upgrading to real embeddings".
sentence-transformers>=3.0

# Tests
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
